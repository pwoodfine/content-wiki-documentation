# seL4 Foundation | Fundación seL4

The architecture uses a mathematically verified C-based microkernel. This component acts as the absolute root of trust. It isolates all hardware resources. The foundation executes underneath the Rust capability manager.
